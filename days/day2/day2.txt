⭐ DAY 2 — ARRAYS & ARRAY METHODS (Beginner Friendly)
Today you will learn:
What is an Array
Why we need Arrays
How to create Arrays
Access values from Arrays
Update values
Important Array Methods
push
pop
map
filter
Practice examples



✅ 1. What is an Array? (Very Simple)
An array is a list where you can store multiple values in one variable.
❌ Without Array
let student1 = "Rahul";
let student2 = "Amit";
let student3 = "Kaushal";



✅ With Array
let students = ["Rahul", "Amit", "Kaushal"];
✔ One variable
✔ Many values
✔ Ordered list



✅ 2. Why Arrays are Needed
Store multiple items
Loop through data
Used everywhere (API data, users, products)
Real-life example:
let cartItems = ["Laptop", "Mouse", "Keyboard"];



✅ 3. Create an Array
let numbers = [10, 20, 30, 40];
let cities = ["Agra", "Delhi", "Mumbai"];
let mixed = [1, "Hello", true];



✅ 4. Access Array Values (Index Concept)
Arrays start from index 0
let fruits = ["Apple", "Banana", "Mango"];

console.log(fruits[0]); // Apple
console.log(fruits[1]); // Banana
console.log(fruits[2]); // Mango
📌 Formula:
arrayName[index]




✅ 5. Update Array Value
let fruits = ["Apple", "Banana", "Mango"];

fruits[1] = "Orange";

console.log(fruits);
// ["Apple", "Orange", "Mango"]




✅ 6. Array Length
let fruits = ["Apple", "Banana", "Mango"];

console.log(fruits.length); // 3
⭐ IMPORTANT ARRAY METHODS



✅ 7. push() — Add item at END
let fruits = ["Apple", "Banana"];

fruits.push("Mango");

console.log(fruits);
// ["Apple", "Banana", "Mango"]



✅ 8. pop() — Remove item from END
let fruits = ["Apple", "Banana", "Mango"];

fruits.pop();

console.log(fruits);
// ["Apple", "Banana"]



✅ 9. map() — Transform each item (IMPORTANT)
👉 map() creates a new array
Example: multiply each number by 2
let numbers = [1, 2, 3];

let result = numbers.map(function(num) {
    return num * 2;
});

console.log(result);
// [2, 4, 6]
🧠 Think:
"Do something to each item"



✅ 10. filter() — Keep only matching items
👉 filter() removes unwanted data
Example: keep numbers greater than 18
let ages = [10, 20, 25, 15];

let adults = ages.filter(function(age) {
    return age >= 18;
});

console.log(adults);
// [20, 25]
🧠 Think:

"Filter means select"
🔁 map vs filter (Very Important)
Method	Purpose
map	Change data
filter	Select data


⭐ Arrow Function (Simple Way)
Same code shorter:
let numbers = [1, 2, 3];

let doubled = numbers.map(num => num * 2);

console.log(doubled);

✅ reduce() — Combine all values into ONE (Simple)
🧠 Think:
reduce = “reduce many values into one value”
Example: Add all numbers
let numbers = [1, 2, 3, 4];

let sum = numbers.reduce((total, num) => {
  return total + num;
}, 0);

console.log(sum); // 10
👉 Step by step:
total starts from 0
0 + 1 = 1
1 + 2 = 3
3 + 3 = 6
6 + 4 = 10
Real-life example (Cart total)
let cart = [100, 200, 300];

let totalPrice = cart.reduce((total, price) => total + price, 0);

console.log(totalPrice); // 600
✅ find() — Find FIRST matching item
🧠 Think:
filter = many results
find = only ONE result
let users = [
  { name: "Rahul", age: 20 },
  { name: "Amit", age: 25 }
];

let user = users.find(u => u.age > 21);

console.log(user);
// { name: "Amit", age: 25 }

🎯 DAY 2 SUMMARY

Method	Use
map	        change each item
filter	    select items
reduce	    combine values
find	    find one item